{
  "name": "project-auth-parent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "google-map-react": "^2.2.0"
  }
}
